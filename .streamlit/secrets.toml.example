# Пример секретов для онлайн-демо. НЕ коммить реальный secrets.toml
# (он в .gitignore). На Streamlit Community Cloud вставь содержимое ниже в
# Settings → Secrets, заменив значение на свой пароль.

demo_password = "choose-a-strong-shared-password"
