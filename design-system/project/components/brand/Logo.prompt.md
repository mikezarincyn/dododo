The Dododo lowercase wordmark with confetti accents.

```jsx
<Logo height={40} assetBase="../../assets" />
```

- Set `assetBase` to the relative path of the `assets/` folder from your file.
- Works on white or any soft tinted surface; never recolour the wordmark.
